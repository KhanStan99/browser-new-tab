# browser-new-tab
A single-file HTML browser homepage: bookmarks in collapsible masonry groups + sticky notes, all in localStorage (never in the file, so it's safe to share). No build, no server, no tracking. Import your existing browser bookmarks, daily photo backdrop, dark mode, drag-and-drop reordering, one-click JSON backup.
